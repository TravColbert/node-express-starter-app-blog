extends ../layout

block articleMeta
  if title 
    h2.shadowed= title

  if errors
    .alert.alert-danger
      each error in errors
        p= error.message
  if coverImage 
    img.cover-image(src=`/images/${coverImage}`)
  
block aside 
  +tagList(tags)

block content
  +articleList(articles)